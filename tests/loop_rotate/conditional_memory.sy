int a[4] = {1, 2, 3, 4};

int main() {
  int i = 0;
  int flag = 0;
  int sum = 0;
  while (i < 4) {
    if (flag != 0) {
      sum = sum + a[i];
    }
    i = i + 1;
  }
  putint(sum);
  putch(10);
  return 0;
}
