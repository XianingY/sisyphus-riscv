int kernel[9];

int main() {
  int i = 0;
  while (i < 9) {
    kernel[i] = (i % 3) - 1;
    i = i + 1;
  }

  i = 0;
  int sum = 0;
  while (i < 9) {
    sum = sum + kernel[i];
    i = i + 1;
  }

  putint(sum);
  putch(10);
  return 0;
}
